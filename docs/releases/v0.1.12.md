# QuotaBar v0.1.12

## Changes

- Makes compact account and limit-type cards readable in both light and dark macOS appearances.
- Replaces the old README screenshots with current renders of the one-row account UI.
- Updates all five README language variants with accurate screenshot alt text and captions.

## Verification

- `swift build --product QuotaBar`
- `swift build --product QuotaBarPreview`
- `swift run QuotaBarChecks`
- `git diff --check`
- Screenshots rendered from the current `QuotaMonitorView` using the explicit `QuotaBarPreview` fixture.
