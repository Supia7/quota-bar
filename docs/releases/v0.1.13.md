# QuotaBar v0.1.13

## Changes

- Adds a real Settings/OAuth screenshot to the README and all translated README variants.
- Makes Claude and Codex OAuth sign-in buttons use explicit blue/white contrast in light and dark appearances.
- Keeps the Settings screenshot privacy-safe: it shows no connected accounts or credentials.

## Verification

- `swift build --product QuotaBar`
- `swift build --product QuotaBarPreview`
- `swift run QuotaBarChecks`
- `git diff --check`
- Settings screenshot rendered from the current `QuotaSettingsView` with an empty registry fixture.
